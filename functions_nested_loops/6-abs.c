#include "main.h"

/**
 *_abs -prints absolute value of a number
 *@a: An integre input
 *trying to make betty happy
 *Return: Absolut value
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
}
