#include "main.h"
/**
 * mul - multiplies two ints
 * @a: first int to be multied
 * @b: second into to be multied
 *
 *Return: The total of the multiplication
 */

int mul(int a, int b);
{
	return (a * b);
}
